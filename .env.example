# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=

# Comma-separated Telegram chat IDs allowed to interact with the bot
TELEGRAM_ALLOWED_CHAT_IDS=

# Working directory for Claude Code sessions (defaults to current directory)
CLAUDE_WORKING_DIR=

# Port for the notification HTTP server (default: 7777)
NOTIFICATION_PORT=7777

# Anthropic API key (if not already set globally)
# ANTHROPIC_API_KEY=
