{
  "name": "tercen-claude-telegram",
  "version": "0.1.0",
  "description": "Two-way Telegram bridge for Claude Code — chat, monitor, and get notifications",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node --env-file=.env dist/index.js",
    "dev": "tsc --watch & node --env-file=.env --watch dist/index.js"
  },
  "keywords": ["telegram", "claude", "claude-code", "bot", "bridge"],
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "grammy": "^1.35.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
